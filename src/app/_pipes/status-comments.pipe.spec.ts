import { StatusCommentsPipe } from './status-comments.pipe';

describe('StatusCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
